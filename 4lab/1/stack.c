#include <stdio.h>

#define SIZE 1000000

int main()
{
	int i, n;
	FILE *fin, *fout;
	fin = fopen("stack.in", "r");
	fout = fopen("stack.out", "w");

	fscanf(fin, "%d\n", &n);
	for (i = 0; i < n; i++)
	{
		if 
	}
}
